# Project Setup

## Overview
This project has been updated with refreshed dependencies and configurations.

## Setup Instructions

### 1. Get Dependencies
Run the following command to fetch all required packages:

```bash
flutter pub get

### 2. Run the Application
After successfully getting the dependencies, start the application with:

```bash
flutter run

**Notes:**
- Make sure you have Flutter installed and configured on your system
- Ensure all dependencies are properly resolved before running the app
